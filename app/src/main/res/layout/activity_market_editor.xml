<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".controller.activity.EditorActivity">

    <!-- 顶部栏 -->
    <RelativeLayout

        android:id="@+id/title_bar_detail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/material_dynamic_neutral_variant70"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <!-- 返回按钮 -->
        <Button
            android:id="@+id/btn_back_to_main_market"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:background="?attr/selectableItemBackground"
            android:text="返回" />
        <!-- 标题文本 -->
        <TextView
            android:id="@+id/title_text_detail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="编辑商品详情"
            android:textColor="#000"
            android:textSize="20sp" />
    </RelativeLayout>

    <!-- 价格 -->
    <EditText
        android:id="@+id/market_price"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="请输入商品价格"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/title_bar_detail"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
    <!-- 名称 -->
    <EditText
        android:id="@+id/market_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="商品名称"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/market_price"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 商品描述 -->
    <EditText
        android:id="@+id/market_context"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:hint="请输入商品描述"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/market_name"
        app:layout_constraintBottom_toTopOf="@id/btn_market_picture"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- Submit Button -->

    <!-- Image Selector Button -->

    <!-- Image Preview -->
    <Button
        android:id="@+id/btn_market_submit"
        android:layout_width="170dp"
        android:layout_height="50dp"
        android:text="提交"
        android:background="@drawable/rounded_button_background"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintLeft_toRightOf="@id/btn_market_picture"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/market_context" />

    <Button
        android:id="@+id/btn_market_picture"
        android:layout_width="170dp"
        android:layout_height="50dp"
        android:text="添加商品照片"
        android:drawableLeft="@android:drawable/ic_menu_gallery"
        android:drawablePadding="8dp"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@drawable/rounded_button_background"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toBottomOf="@id/market_context"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintHorizontal_bias="1.0" />


    <ImageView
        android:id="@+id/iv_image"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/btn_market_picture"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="H,16:9" />

</androidx.constraintlayout.widget.ConstraintLayout>